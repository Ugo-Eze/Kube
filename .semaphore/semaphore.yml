version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Terraform
    task:
      jobs:
        - name: Initialize terraform
          commands:
            - checkout
            - terraform init
      secrets:
        - name: do-k8s
  - name: Terraform
    task:
      jobs:
        - name: Get terraform plan
          commands:
            - terraform plan
after_pipeline:
  task:
    jobs:
      - name: 'Job #1'
        commands: []
